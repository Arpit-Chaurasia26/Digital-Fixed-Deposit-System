# Digital Fixed Deposit System Backend

## Tests

Run the unit tests with Maven:

```zsh
./mvnw test
```

If you want to run a specific test class:

```zsh
./mvnw -Dtest=FixedDepositServiceTest test
```
